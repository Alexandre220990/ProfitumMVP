Corrections à appliquer manuellement dans server/src/routes/experts.ts:

ERREUR LIGNE 56: No value exists in scope for the shorthand property 'name'
ERREUR LIGNE 59: Cannot find name 'name'
ERREUR LIGNE 72: No value exists in scope for the shorthand property 'name'
ERREUR LIGNE 96: No value exists in scope for the shorthand property 'name'

Ces erreurs indiquent que la variable `name` est utilisée mais n'existe plus.
Dans le destructuring, nous avons remplacé `name` par `first_name` et `last_name`.

SOLUTION:
Remplacer toutes les utilisations de `name` par une construction basée sur `first_name` et `last_name`:
  name: `${first_name || ''} ${last_name || ''}`.trim() || company_name

LIGNES 189, 274, 444: Property 'first_name' does not exist...
Ces erreurs indiquent que les SELECT ne récupèrent pas first_name/last_name de la BDD.
Il faut corriger les SELECT pour inclure ces champs.

